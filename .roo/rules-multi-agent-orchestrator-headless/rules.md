# multi-agent-orchestrator-headless mode rules

Before this mode calls `gh issue create` (as part of a full-cycle
explore→file→implement pipeline), read
`.roo/rules-task-filing/rules.md` in full and apply its size ceiling,
umbrella/sub-issue pattern, exact-location requirement, and
pre-filing self-check to every issue filed.
